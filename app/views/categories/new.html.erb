<h1>create a category</h1>

<%= form_for(@category) do |f| %> 
    <%= f.label :name, "Category name: " %>
    <%= f.text_field :name %>
    <button type=f.submit >Create categoty</button>
    <% end %>