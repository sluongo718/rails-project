<h1>create a category</h1>

<% if flash[:error] %>
    <%= flash[:error] %>
<% end %>


<%= form_for(@category) do |f| %> 
    <%= f.label :name, "Category name: " %>
    <%= f.text_field :name %>
    <button type=f.submit >Create category</button>
    <% end %>